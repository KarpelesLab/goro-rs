[package]
name = "goro-sapi"
version = "0.1.0"
edition = "2024"
description = "SAPI trait and implementations for goro-rs"

[dependencies]
goro-core = { path = "../goro-core" }
goro-vfs = { path = "../goro-vfs" }
